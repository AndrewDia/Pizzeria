<div class="promotion">
    <h2>{{$promo->name}}</h2>
    <div class="action">
        <img src="img/promo/{{$promo->img_name}}" alt="promotion">
    </div>
    <h3>{{$promo->description}}</h3>
</div>
